package com.reservas.backend.appointment;

/** Debe calzar con AppointmentStatus del frontend (frontend/src/types/appointment.ts). */
public enum AppointmentStatus {
    CONFIRMED,
    PENDING,
    CANCELLED,
    COMPLETED
}
